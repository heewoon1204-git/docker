# docker network

## docker network 필요성
도커 내부 컨테이너끼리 통신하기 위해 컨테이너별로 ip를 주고, 서로 통신할 수 있는 네트워크를 만들어줌

```bash
                Docker Network

      nginx  <------>  spring  <------> postgres
  (172.17.0.2)      (172.17.0.3)      (172.17.0.3)
```
host의 네트워크와 docker 내부의 네트워크는 별도의 네트워크 
docker 컨테이너에서 외부로 나가기 위해서는 게이트웨이를 통해야함.
```bash
            AWS VPC                              host network
        (172.31.0.0/16) -> ec2를 위한 큰 네트워크
                │
        Internet Gateway
                │
                ▼
           EC2 (ens5)
        (172.31.44.71) -> aws vpc와 연결된 실제 ec2 네트워크 인터페이스
                │
                │ Docker Engine
                ▼
  --------------------------------------------------------------
                                                  docker network
            docker0 -> 가상 네트워크 스위치(bridge)
         (172.17.0.1) -> 컨테이너들이 사용하는 게이트웨이 역할
               │         컨테이너들을 위한 작은 네트워크
               │
        ┌──────┴───────┐
        │              │
        ▼              ▼
    172.17.0.2   172.17.0.3
      nginx        spring
```
잠깐!!
gateway는 밖으로 나갈 때 반드시 거쳐야 하는 출입문. 현관문 같은거!
```bash
방 (container)
    ↓
현관 (gateway)
    ↓
복도 (host)
    ↓
밖 (internet)
```

## docker network
docker를 설치하면 기본적으로 3개의 네트워크를 만들어줌
1. bridge
2. host
3. none

### 1. docker bridge network
- docker의 기본 네트워크. 가상 스위치. 
- docker 내부 네트워크기 때문에 외부 pc로는 접속할 수 없음. host를 통해서 접근해야함.
- docker run 같은 명령어 실행 시 bridge로 연결
  명령어 실행 시 --network 옵션을 적지 않으면 기본적으로 bridge 네트퉈크에 연결함

```bash
# 격리된 네트워크를 만들고, 그 안에서 서버 혹은 컨테이너끼리 통신한다는 개념이 docker bridge, vpc 끼리 동일
docker 버전
           docker0 -> 가상 스위치 역할
        ┌─────┼─────┐
        │     │     │
     nginx  spring postgres

aws 버전
             vpc 
        ┌─────┼─────┐
        │     │     │
     ec2 1  ec2 2 ec2 3

```

### bridge notwork에 연결된 container 설정보기
docker run 명령어로 실행하면 자동으로 bridge network에 연결된 상태로 contianer가 실행됨.

```bash
# 명령어
docker inspect web(container name)

# 결과
"Networks": {
    "bridge": {   -> 해당 container가 bridge 네트워크에 연결되어 있음을 보여줌
        "IPAMConfig": null,
        "Links": null,
        "Aliases": null,
        "DriverOpts": null,
        "GwPriority": 0,
        "NetworkID": "fec8b5c677ac3dc36197616c7142e60aa067c796eea33b1439f34d56eae8b6e7",
        "EndpointID": "cd41854258ef2cc5afe2e14bdfd90a6bcd1dcb6568321094d60b2f6288ef4423",
        "Gateway": "172.17.0.1", -> container가 나가기 위해 거쳐야하는 문
        "IPAddress": "172.17.0.2", -> docker가 container에게 준 주소
        "MacAddress": "da:63:1d:be:44:98",
        "IPPrefixLen": 16, -> 172.17.0.0/16을 의미
        "IPv6Gateway": "",
        "GlobalIPv6Address": "",
        "GlobalIPv6PrefixLen": 0,
        "DNSNames": null
    }
}
```

### 2. docker host network
- bridge를 쓰지 않고 컨테이너가 host의 네트워크를 그대로 사용
  즉 host의 ip를 컨테이너 ip로 사용. 중간에 docker0 없음.
- 네트워크 성능이 좋지만, 격리가 사라지는 단점이 생김
  따라서 성능이 매우 중요한 특수한 경우만 사용

### 3. docker none network
- 말그대로 네트워크가 없음.
- 파일 변환, 암호화, 영상 인코딩 등 네트워크가 필요없는 작업을 하는 컨테이너에서 사용. 
- 보안상 안전
```bash
    Container
        ↓
    IP 없음
        ↓
    인터넷 안 됨
        ↓
다른 컨테이너와 통신 안 됨
```

### 4. user-defined bridge 네트워크 생성한 뒤 호출해보기(실무 많이 사용!)
- 실무에서는 ip로 호출하지 않고 dns 이름으로 호출함.
- 컨테이너가 다시 실행되면 ip는 계속 변경되기 때문
  
4-1. docker network 생성하기
```bash
# 명령어
docker network create my-network

# 네트워크 생성 확인
docker network ls

# 결과
NETWORK ID     NAME         DRIVER    SCOPE
fec8b5c677ac   bridge       bridge    local
b60a683f901a   host         host      local
87b5122a3180   my-network   bridge    local -> 신규 생성한 네트워크
8fd0839bf891   none         null      local
```
```bash
# 명령어
docker inspect web(container name)

# 결과
"Networks": {
    "my-network": {   -> bridge가 아니라 내가 생성한 network로 연결
        "IPAMConfig": null,
        "Links": null,
        "Aliases": null,
        "DriverOpts": null,
        "GwPriority": 0,
        "NetworkID": "87b5122a3180068a345d7553f2004a69cdd644cd64ce63930d25c088affc40e5",
        "EndpointID": "bbe62e8032d8f369633d6fd556120c2cc405d529aa44e34ef32034ee64d34423",
        "Gateway": "172.18.0.1",
        "IPAddress": "172.18.0.2",
        "MacAddress": "2e:e6:bd:bb:fd:ee",
        "IPPrefixLen": 16,
        "IPv6Gateway": "",
        "GlobalIPv6Address": "",
        "GlobalIPv6PrefixLen": 0,
        "DNSNames": [  -> 컨테이너를 실해알 때 docker가 dns를 설정. 따라서 dns name으로 호출 가능
            "web", -> 컨테이너 생성할 때 부여한 이름          
            "5761b4e69c13" -> 컨테이너 Id. 컨테이너 id도 dns 이름으로 사용할 수 있음.
        ]
    }
}

# 컨테이너 정보
CONTAINER ID   IMAGE     COMMAND                  CREATED          STATUS          PORTS     NAMES
059938e91843   alpine    "sh"                     25 minutes ago   Up 25 minutes             test
5761b4e69c13   nginx     "/docker-entrypoint.…"   25 minutes ago   Up 25 minutes   80/tcp    web

```

4-2. 신규로 생성한 네트워크로 컨테이너 실행하기
신규 생성한 my-network를 이용해서 컨테이너 실행하기

```bash
docker run -d \
--name web \
--network my-network \
nginx

docker run -dit \
--name test \
--network my-network \
alpine sh
```

4-3. 컨테이너끼리 ip 없이 호출해보기
같은 네트워크를 사용해서 실행한 컨테이너 중 alpine 컨테이너에서 ip 없이 nginx 호출하면 docker 내부 dns가 동작하여 web -> nginx ip를 찾아서 호출함.
컨테이너끼리 호출할 때 ip를 사용하지 않고 모두 이름으로 통신함!!
```bash
# 명령어
wget -O- http://web

# 호출 flow
   web
    ↓
 DNS 조회
    ↓
172.18.0.2
    ↓
 HTTP 요청
```

# docker network 실습
## container 끼리 통신해보기

### 1. 테스트용 컨테이너 생성
가벼운 alpine을 생성하기
- d : 백그라운드에서 실행하기
- i : 표준 입력 유지하기
- t : 터미널 실행하기
- sh : alpine은 bash 대신 sh
```bash
# 컨테이너 실행
docker run -dit --name test alpine sh

# 실행 확인
docker ps
```

### 2. 신규 컨테이너에서 기존 nginx에 ping 보내기
- ping : ICMP 프로토콜을 이용해 상대가 살아있는지 확인하는것.
         http 되는거랑은 별개
         ping이 되면 네트워크 연결은 되어 있다는 뜻
- curl, wget : http 프로토콜로 웹 요청
```bash
# test 컨테이너로 접속하기
docker exec -it test sh

# ping으로 네트워크 연결 확인
ping 172.17.0.2(nginx ip)

# wget으로 http 통신 확인
wget -O- http://172.17.0.2
```

